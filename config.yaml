swagger_server:
  enable: true
  port: 3001

api_server:
  enable: true
  port: 8601

# Redis configurations
redis:
  addr: "localhost:6379"
  password: ""
  db: 5

# Database configurations
db:
  url: 'postgresql://root:123456@localhost:54321/orders?sslmode=disable'
  max_connections: 10
  connection_timeout: 30s
